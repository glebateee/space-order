# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: '3'

vars:
  GREETING: Hello, world!

tasks:
  run:
    desc: run application with local.yaml
    cmds:
      - go run ".\cmd\order\main.go" --config="./config/local.yaml"
  default:
    desc: Print a greeting message
    cmds:
      - echo "{{.GREETING}}"
    silent: true
